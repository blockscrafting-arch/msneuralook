# Аудит: маркеры, мультиканал, отложенная публикация

**Дата:** 2026-02-07  
**Роли:** Руководитель проекта, SMM, CEO, собственник, дизайнер, elite senior (код, безопасность).

---

## 1. Соответствие плану и целям

| Пункт плана | Статус | Комментарий |
|-------------|--------|-------------|
| Таблица keywords, миграция 003 | Выполнено | Создана, UNIQUE по word. |
| Userbot: кеш маркеров, фильтр по тексту | Выполнено | Кеш 30 с, регистронезависимый подстроковый поиск. |
| Админка: CRUD маркеров | Выполнено | Список, добавить, удалить, FSM, audit_log. |
| Таблица target_channels, миграция 004 | Выполнено | Перенос из config. |
| Админка: целевые каналы (список) | Выполнено | Список с удалением, добавление. |
| Публикация во все целевые каналы | Выполнено | publish_to_all_channels, fallback на config. |
| Миграция 005: scheduled_at, статус scheduled | Выполнено | Колонка, CHECK, индекс. |
| Кнопка «Запланировать», FSM даты/времени | Выполнено | ДД.ММ.ГГГГ ЧЧ:ММ (МСК), проверка «в будущем». |
| Фоновый планировщик | Выполнено | Каждые 30 с, audit_log, отмена в main. |

**Итог:** Все запланированные функции реализованы.

---

## 2. Руководитель проекта (дотошность)

- **Миграции:** Есть 003, 004, 005; порядок применения документирован в DEPLOY/PROJECT. Рекомендация: добавить в PROJECT.md раздел «Миграции» с командами применения.
- **Обратная совместимость:** При отсутствии таблицы target_channels или keywords код не падает (get_active_target_channels, get_all_target_channels, get_scheduled_posts_due — try/except или проверки). Userbot: при отсутствии таблицы keywords запрос упадёт с UndefinedTableError — нужна обработка в userbot (см. п. 6).
- **Откат:** Нет обратных миграций (rollback) для 003–005; для текущего этапа приемлемо.

---

## 3. SMM (удобство редактора)

- **Чёткость действий:** Опубликовать / Запланировать / Редактировать / Отклонить — понятно.
- **Запланировать:** После ввода времени показывается подтверждение «Пост #N запланирован на ДД.ММ.ГГГГ ЧЧ:ММ (МСК)». Кнопки под исходным сообщением остаются — редактор может по ошибке нажать «Опубликовать»; сработает защита «Пост уже обработан», но лучше убирать клавиатуру после выбора «Запланировать» (см. п. 7).
- **Формат даты:** Один явный формат (ДД.ММ.ГГГГ ЧЧ:ММ) и пример в подсказке — хорошо.
- **Мультиканал:** Редактор не выбирает канал — публикация идёт во все целевые; это соответствует ТЗ.

---

## 4. CEO / собственник

- **Цели ТЗ:** Фильтр по маркерам, несколько каналов, отложенная публикация — закрыты.
- **Риски:** Зависимость от одной БД и одного инстанса editor-bot; при падении планировщика запланированные посты не публикуются до перезапуска. Для критичных дедлайнов можно добавить мониторинг (health-check по последнему успешному тику) или внешний cron.
- **Масштаб:** Добавление новых целевых каналов и маркеров — без деплоя, через админку.

---

## 5. Дизайнер (UX)

- **Консистентность:** Стиль кнопок (✅ / 📅 / ✏ / ❌) и списков в админке выдержан.
- **Четыре кнопки в одном ряду:** Сейчас два ряда по две кнопки — читаемо, перегрузки нет.
- **Админка:** «Целевые каналы» и «Слова-маркеры» в главном меню на одном уровне с остальными — логично.
- **Сообщения об ошибках:** Короткие, по делу («Неверный формат», «Укажите время в будущем», «Пост уже обработан»).

---

## 6. Качество кода и безопасность (elite senior)

**Плюсы:**

- Параметризованные запросы (asyncpg), нет конкатенации SQL.
- Ввод каналов и маркеров нормализуется (strip, lower для ключевых слов).
- HTML-экранирование в админке (_esc) при выводе в бот (parse_mode=HTML).
- Проверка пути к PDF (_is_path_safe) против path traversal.
- Валидация post_id: int, >= 1, из callback_data.
- Audit_log на все значимые действия (approved, rejected, scheduled, scheduled_published, admin_*).

**Замечания и рекомендации:**

1. **Userbot и таблица keywords:** Если миграция 003 не применена, при первом сообщении из канала вызов `get_keywords(pool)` приведёт к `UndefinedTableError`. Рекомендация: в userbot в `source_channels.get_keywords` обернуть запрос в try/except asyncpg.UndefinedTableError и возвращать [].
2. **Гонка при «Опубликовать»:** Два редактора могут одновременно нажать «Опубликовать» по одному посту; оба проходят проверку status == 'pending_review', затем оба публикуют. Рекомендация: атомарно обновлять статус (UPDATE posts SET status = 'published' WHERE id = $1 AND status = 'pending_review' RETURNING id) и публиковать только если вернулась строка.
3. **Клавиатура после «Запланировать»:** После нажатия «Запланировать» убрать кнопки с сообщения (edit_reply_markup(None)) в cb_schedule, чтобы не вводить редактора в заблуждение.
4. **Часовой пояс:** Явно задан Europe/Moscow; при смене политики по часовым поясам достаточно поменять константу MSK.

---

## 7. Внесённые правки по аудиту (выполнены)

- **Userbot:** В `source_channels.get_keywords` добавлен try/except asyncpg.UndefinedTableError, возврат [] при отсутствии таблицы.
- **review.py cb_schedule:** После перехода в FSM вызывается `callback.message.edit_reply_markup(reply_markup=None)` (в try/except), чтобы убрать кнопки и избежать повторных нажатий.
- **review.py cb_approve:** Введена атомарная операция `claim_pending_for_publish(pool, post_id)` в repository: `UPDATE posts SET status = 'published' WHERE id = $1 AND status = 'pending_review'`. Публикация выполняется только при успешном обновлении; при ошибке публикации статус откатывается в `pending_review`.
