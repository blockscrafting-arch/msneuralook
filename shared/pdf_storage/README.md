# PDF Storage

Каталог для PDF-файлов, скачанных из постов. Заполняется в рантайме (userbot, n8n).

- Содержимое (`*`) в **.gitignore** — в репозиторий не коммитится.
- `.gitkeep` нужен, чтобы пустая папка попала в git при клоне.

Volume монтируется в `docker-compose` для userbot и n8n.
